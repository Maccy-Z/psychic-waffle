export AMGX_DIR=/home/maccyz/Documents/AMGX/

python setup.py build_ext --inplace

Make clean conda env with python 3.11

Run:
conda install cudatoolkit(==11.7)
conda install cudatoolkit-dev

Install gcc 11 and g++ 11
Set so conda env uses gcc 11

ln -s $(conda info --base)/envs/{your_env_name}/bin/x86_64-conda_cos6-linux-gnu-gcc $(conda info --base)/envs/{your_env_name}/bin/gcc
ln -s $(conda info --base)/envs/{your_env_name}/bin/x86_64-conda_cos6-linux-gnu-g++ $(conda info --base)/envs/{your_env_name}/bin/g++
